import './courses.css';
import Nav from '../Components/Nav/Nav.js';

const Courses = () => {
    return (
        <div className="Courses">
            <Nav page="Courses" />
        </div>
    );
};

export default Courses;
